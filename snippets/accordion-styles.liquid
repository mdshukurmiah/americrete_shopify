{%- doc -%}
  Styles for the accordion and accordion row components. Extracted into a
  snippet so that blocks which use accordion markup without nesting inside an
  accordion block (e.g. disclosures) can include these styles independently,
  ensuring they are sent to the client when CSS subsetting is enabled.
{%- enddoc -%}

{% stylesheet %}
  .accordion {
    display: flex;
    flex-direction: column;
    gap: var(--item-gap, 0);
    flex: 1;
    width: 100%;
  }

  accordion-custom {
    display: block;
    width: 100%;
  }

  .accordion--dividers accordion-custom:not(:first-child) .details {
    border-block-start: var(--style-border-width) solid var(--color-border);
  }

  /* When accordion borders are not set, show fallback borders */
  .accordion--dividers {
    /* stylelint-disable-next-line declaration-property-value-disallowed-list */
    --show-fallback-borders: 0;
  }

  .accordion--dividers:not([class*='color-'])[style*='--border-width: 0'],
  .accordion--dividers:not([class*='color-'])[style*='--border-style: none'] {
    --show-fallback-borders: 1;
  }

  .accordion--dividers accordion-custom:first-child .details {
    border-block-start: calc(var(--style-border-width) * var(--show-fallback-borders)) solid var(--color-border);
  }

  .accordion--dividers accordion-custom:last-child .details {
    border-block-end: calc(var(--style-border-width) * var(--show-fallback-borders)) solid var(--color-border);
  }

  .accordion--dividers .details-content {
    padding-block-end: var(--padding-sm);
  }

  .accordion--caret .icon-plus,
  .accordion--plus .icon-caret {
    display: none !important;
  }

  /* because we can't pass apply a specific class on a block based on its parent block setting */
  .accordion .details__header {
    font-family: var(--summary-font-family, var(--font-family));
    font-style: var(--summary-font-style, var(--font-style, normal));
    font-weight: var(--summary-font-weight, var(--font-weight));
    font-size: var(--summary-font-size, var(--font-size));
    line-height: var(--summary-font-line-height, var(--line-height));
    text-transform: var(--summary-font-case, var(--text-transform));
    letter-spacing: var(--letter-spacing, normal);
    min-height: var(--minimum-touch-target);
  }

  .details__icon {
    height: auto;
    margin-inline-end: var(--margin-xs);
  }

  .accordion--icon-stacked .details .svg-wrapper {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background-color: var(--accordion-inactive-bg, rgba(0, 0, 0, 0.06));
    color: var(--accordion-inactive-icon-color, currentColor);
    transition: background-color 0.25s ease, color 0.25s ease, transform 0.25s ease;
  }

  .accordion--icon-stacked .details[open] .svg-wrapper {
    background-color: var(--accordion-active-bg, #000000);
    color: var(--accordion-active-icon-color, #ffffff);
  }

  /* Accordion Item custom styles */
  .accordion accordion-custom .details {
    border-style: var(--item-border-style, initial);
    border-width: var(--item-border-width, initial);
    border-color: var(--item-border-color, initial);
    border-radius: var(--item-border-radius, initial);
    overflow: hidden;
  }

  .accordion accordion-custom .details__header {
    padding-top: var(--item-padding-top, 0);
    padding-bottom: var(--item-padding-bottom, 0);
    padding-left: var(--item-padding-left, 0);
    padding-right: var(--item-padding-right, 0);
  }

  .accordion accordion-custom .details-content {
    padding-left: var(--item-padding-left, 0);
    padding-right: var(--item-padding-right, 0);
    padding-bottom: var(--item-padding-bottom, 0);
  }
{% endstylesheet %}
